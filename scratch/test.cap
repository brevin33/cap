
struct Foo{
	i32* x
	i32* y
}

void* alloc(u64 size, u32 alignment){
	return alloc(size, alignment)
}

program test_program{
	Foo f
	i32*@f x = alloc(i32)
	f.x = x
	f.y = alloc(i32)
	f.x* = 10
	f.y* = 20
	return f.y*
}
