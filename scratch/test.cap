
struct Foo{
	i32 val
}

struct Point{
	i32* x
	i32* y
}

void* alloc(Foo* foo, u64 size, u32 alignment){
	return alloc(size, alignment)
}

program test_program{
	Foo@stack f
	i32*@f x = alloc(i32)
	Point p
	p.x = alloc(i32)
	p.y = alloc(i32)
	p.x* = 10
	p.y* = 20
	return p.y*
}
